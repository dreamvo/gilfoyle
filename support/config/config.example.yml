services:
  db:
    dialect: postgres
    host: localhost
    port: 3692
    user: root
    password: root
    database: gilfoyle
  redis:
    host: localhost
    port: 3482
    password: ''
  ipfs:
    gateway: gateway.ipfs.io
settings:
  maxFileSize: 50mb
